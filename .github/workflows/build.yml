name: Build Windows Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Download Node.exe
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path tools | Out-Null
          Invoke-WebRequest https://nodejs.org/dist/v22.2.0/win-x64/node.exe -OutFile tools/node.exe

      - name: Build executable
        shell: pwsh
        run: |
          pyinstaller --noconfirm --clean --windowed --onefile `
            --name D2RMarketplace `
            --icon=icon.ico `
            --add-data "templates;templates" `
            --add-data "static;static" `
            --add-data "data;data" `
            --add-data "database;database" `
            --add-data "tools;tools" `
            --add-data "config.json;." `
            desktop_launcher.py

      - name: Rename executable
        shell: pwsh
        run: |
          Rename-Item dist/D2RMarketplace.exe D2RMarketplace-windows.exe

      - name: Create ZIP
        shell: pwsh
        run: |
          Compress-Archive -Path dist/D2RMarketplace-windows.exe -DestinationPath D2RMarketplace-windows.zip

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: D2RMarketplace-windows.zip
